# Core (already have)
huggingface_hub
python-dotenv
openai

# PDF reading (pick one — pdfplumber is better)
pdfplumber

# Voice output fallback (only needed if espeak-ng not installed on system)
pyttsx3

# Optional: faster PDF fallback if pdfplumber fails
pdfminer.six

#textual
#textual>=0.80.0

#promt toolkit
#prompt_toolkit>=3.0.0
